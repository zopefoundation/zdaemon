# Sample config file for zdctl.py and zdrun.py (which share a schema).

<zdctl>
  # This is a pure example
  program sleep 100
</zdctl>

<zdrun>
  backoff-limit 10
  daemon	True
  forever	True
  socket-name	zdsock
  exit-codes	0,2
  # user has no default
  directory	/
</zdrun>
